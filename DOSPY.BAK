#include<stdio.h>
#include<conio.h>
void main()
{
	int n,rem,sum=0,spy=1;
	clrscr();
	printf("n=");
	scanf("%d",&n);

	do
	{
		rem=n%10;
		sum=sum+rem;
		spy=spy*rem;
		n=n/10;

	}
	while(n>0);
	{
		printf("sum=%d",sum);
		printf("\nspy=%d",spy);
	}

	 if(sum==spy)
	 {
		printf("\nits spy");

	 }
	 else
	 {
		printf("\nits not spy");

	 }






	getch();


}
