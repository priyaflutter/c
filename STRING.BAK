#include<stdio.h>
#include<conio.h>
void main()
{

	char a[100];
	int i,c=0,ch=0,uc=0,lc=0,v=0,d=0,sc=0,s=0;
	clrscr();
	printf("enter string=");
	gets(a);
	puts(a);

	for(i=0;a[i]!=NULL;i++)
	{
			c++;

	}

		printf("total char=%d",c);

	for(i=0;a[i]!=NULL;i++)
	{
		if ((a[i]>=65 && a[i]<=90) || (a[i]>=97 && a[i]<=122))
		{
			ch++;

		}

	}
		printf("\nTotal character=%d",ch);

	for(i=0;a[i]!=NULL;i++)
	{
		if (a[i]>=65 && a[i]<=90)
		{
			uc++;

		}

	}
		printf("\ntotal uper char=%d",uc);

	for(i=0;a[i]!=NULL;i++)
	{
		if (a[i]>=97 && a[i]<=122)
		{
			lc++;

		}

	}
		printf("\ntotal lower char=%d",lc);

	for(i=0;a[i]!=NULL;i++)
	{
		if (a[i]==65 || a[i]==69 || a[i]==73 || a[i]==79 || a[i]==85 || a[i]==97 || a[i]==101 || a[i]==105 || a[i]==111 || a[i]==117)
		{
			v++;

		}

	}
		printf("\ntotal vowels=%d",v);

	for(i=0;a[i]!=NULL;i++)
	{
		if (a[i]>=48 && a[i]<=57)
		{
			d++;

		}

	}
		printf("\ntotal digit=%d",d);

	for(i=0;a[i]!=NULL;i++)
	{
		if ((a[i]>=33 && a[i]<=47) || (a[i]>=58 && a[i]<=64) || (a[i]>=91 && a[i]<=96) || (a[i]>=123 && a[i]<=126))
		{
			sc++;

		}

	}
		printf("\ntotal special char=%d",sc);

	for(i=0;a[i]!=NULL;i++)
	{
		if (a[i]==32)
		{
			s++;

		}

	}
		printf("\ntotal space=%d",s);






	getch();

}